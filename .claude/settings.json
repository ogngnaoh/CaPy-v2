{
  "permissions": {
    "allow": [
      "Bash(make *)",
      "Bash(pytest *)",
      "Bash(python scripts/*)",
      "Bash(ruff *)",
      "Bash(black *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "ruff check --fix \"$TOOL_INPUT_file_path\" 2>/dev/null; ruff format \"$TOOL_INPUT_file_path\" 2>/dev/null; true"
      }
    ],
    "Stop": [
      {
        "command": "CHANGED=$(git diff --name-only HEAD~1 2>/dev/null | grep -c '\\.py\\|\\.yaml\\|\\.md' 2>/dev/null || echo 0); if [ \"$CHANGED\" -gt 3 ]; then echo 'Session had significant changes. Consider running /claude-md-management:revise-claude-md to capture learnings.'; fi; true"
      }
    ]
  }
}
